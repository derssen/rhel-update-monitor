[Unit]
Description=Security Update Monitor Service
After=network.target

[Service]
Type=oneshot
# Run as the service user we created earlier
User=update_checker
# Path to the script
ExecStart=/usr/local/bin/monitor.sh

[Install]
WantedBy=multi-user.target
